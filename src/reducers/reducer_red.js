
const defaultRedTable={
    N:0,
    X:0,
    M:0,
    W:0,
    D:''
}

export default function RedTable(state=defaultRedTable, action) {
    switch (action.type){
        default:
            return state;
    }
}